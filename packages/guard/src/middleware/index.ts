/**
 * Middleware hooks cho `buildHandler` — implement ở `p1-01`.
 *
 * Subpath `./middleware` giữ chỗ từ foundation để consumer không đổi import
 * path khi middleware thật ship. Plan này (p0-01) **chưa** hook handler nào.
 */

export {};
