/**
 * ResultFeed – Vietlott Sources Barrel
 *
 * Gom mọi adapter hiển thị kết quả Vietlott (`vietlott-detail` chính chủ, `minhchinh` sau
 * này, …). Registry (`../registry.ts`) import từ đây, KHÔNG import trực tiếp từng site con.
 */

export { vietlottDetailAdapter } from "./vietlott-detail";
