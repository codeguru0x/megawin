# Player SDK — S3 Upload Configuration
# Copy file này thành .env.s3.local và điền thông tin:
#   cp .env.s3.example .env.s3.local

S3_BUCKET=your-sdk-bucket-name
AWS_PROFILE=your-aws-profile
AWS_REGION=ap-southeast-1
