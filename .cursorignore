# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
.env.*

# GitNexus knowledge-graph index — ~550MB binary (LadybugDB + parse cache).
# Cursor không index thư mục này; truy cập graph qua MCP server `gitnexus`.
.gitnexus/
